apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ai-task-platform
data:
  NODE_ENV: production
  PORT: "5000"
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  DB_NAME: ai_task_platform
  FRONTEND_BASE_URL: http://frontend
